{% extends "CoreBundle:MonColloque:layout-menu.html.twig" %}

{% block title %}
    Confirmation thème - {{ parent() }}
{% endblock %}

{% block MonColloque_content %}
    <div class="bloc">
        <div class="row">
            <div class="col-lg-12">
                <h1>Thème {{ theme.title }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <h3>Êtes-vous sûr de vouloir choisir le thème <strong>{{ theme.title }}</strong> pour votre groupe ?
                </h3>
                <div class="alert alert-info">
                    Nombre de place(s) occupée(s) : {{ nbGroupesActuel }}/{{ theme.nbGroupes }}
                </div>
                <div class="alert alert-danger">
                    Attention, un membre choisi pour toute son équipe. Il est ensuite impossible de retourner en
                    arrière. Soit sûr que toute ton équipe est d'accord avant de confirmer.
                </div>
                <div class="row">
                    <form action="#" method="post">
                        <input type="hidden" value="{{ theme.id }}" name="id">
                        <div class="col-sm-6 text-right">
                            <input type="submit" class="btn btn-success" value="Choisir ce thème">
                        </div>
                        <div class="col-sm-6">
                            <a href="{{ path('core_moncolloque_choosetheme') }}" class="btn btn-danger">Annuler</a>
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>

{% endblock %}